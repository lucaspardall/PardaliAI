 DOCUMENTAÇÃO CLERK EXPRESS 2025 - RESUMO
⚠️ MUDANÇAS IMPORTANTES

NOVO PACOTE: @clerk/express (não mais @clerk/clerk-sdk-node)
DEPRECAÇÃO: req.auth → agora use req.auth() como FUNÇÃO

🚀 INSTALAÇÃO
bashnpm install @clerk/express dotenv
🔑 CONFIGURAÇÃO .env
envCLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
🛡️ MIDDLEWARE BÁSICO
javascriptimport 'dotenv/config'
import express from 'express'
import { clerkMiddleware } from '@clerk/express'

const app = express()

// Middleware básico - anexa auth ao req
app.use(clerkMiddleware())
🔒 PROTEÇÃO DE ROTAS
Opção 1: requireAuth() - Redireciona se não autenticado
javascriptimport { requireAuth, getAuth } from '@clerk/express'

// Protege rota específica
app.get('/protected', requireAuth(), async (req, res) => {
  const { userId } = getAuth(req)
  // userId garantido aqui
})

// Com URL customizada
app.get('/admin', requireAuth({ signInUrl: '/login' }), handler)
Opção 2: getAuth() - Controle manual
javascriptapp.get('/api/user', async (req, res) => {
  const { userId } = getAuth(req)
  
  if (!userId) {
    return res.status(401).json({ error: 'Não autenticado' })
  }
  
  // Buscar dados do usuário
  const user = await clerkClient.users.getUser(userId)
  res.json(user)
})
🔧 CLERK CLIENT
javascriptimport { clerkClient } from '@clerk/express'

// Buscar usuário
const user = await clerkClient.users.getUser(userId)

// Listar usuários
const users = await clerkClient.users.getUserList()
🎯 EXEMPLO COMPLETO CORRIGIDO
javascriptimport 'dotenv/config'
import express from 'express'
import { 
  clerkMiddleware, 
  requireAuth, 
  getAuth,
  clerkClient 
} from '@clerk/express'

const app = express()

// Middleware global
app.use(clerkMiddleware())

// Rota pública
app.get('/', (req, res) => {
  res.send('Home pública')
})

// Rota protegida com requireAuth
app.get('/dashboard', requireAuth(), async (req, res) => {
  const { userId } = getAuth(req)
  const user = await clerkClient.users.getUser(userId)
  res.json({ dashboard: true, user })
})

// API com controle manual
app.get('/api/auth/user', async (req, res) => {
  try {
    const { userId } = getAuth(req)
    
    if (!userId) {
      return res.status(401).json({ 
        error: 'Clerk user data not available' 
      })
    }
    
    const user = await clerkClient.users.getUser(userId)
    res.json({
      id: user.id,
      email: user.emailAddresses[0]?.emailAddress,
      name: user.firstName
    })
  } catch (error) {
    res.status(500).json({ error: error.message })
  }
})

// Middleware customizado
const checkPermission = (req, res, next) => {
  const auth = getAuth(req)
  
  if (!auth.has({ permission: 'org:admin' })) {
    return res.status(403).json({ error: 'Sem permissão' })
  }
  
  next()
}

app.get('/admin', requireAuth(), checkPermission, handler)

app.listen(3000)
📝 TYPESCRIPT
typescript// types/globals.d.ts
import '@clerk/express'

declare global {
  namespace Express {
    interface Request {
      auth: import('@clerk/express').AuthObject
    }
  }
}
⚡ CORREÇÕES PARA SEU ERRO

Trocar req.auth por getAuth(req)
Verificar se userId existe antes de usar
Usar @clerk/express ao invés de @clerk/clerk-sdk-node
Garantir que middleware está aplicado globalmente

Solução rápida para seu erro:
javascript// ❌ ERRADO
const { userId } = req.auth;

// ✅ CORRETO
const { userId } = getAuth(req);