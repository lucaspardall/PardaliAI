✅ Verificação do seu código
Seu trecho:

ts
Copiar
Editar
const urlString = `${baseUrl}${basePathForShopAuthorize}?` + 
                  `partner_id=${encodeURIComponent(this.config.partnerId)}&` + 
                  `timestamp=${encodeURIComponent(timestamp)}&` + 
                  `sign=${encodeURIComponent(signature)}&` + 
                  `redirect=${encodeURIComponent(this.config.redirectUrl)}&` + 
                  `state=${encodeURIComponent(stateParam)}`;
Está correto. O parâmetro timestamp está nomeado certamente certo aqui.

🟥 Onde pode estar o problema então?
1. Editor ou terminal com encoding errado
Se estiver usando console.log e copiando a URL de um terminal (ou do Replit), pode estar ocorrendo substituição invisível de caractere:

timestamp= → ×tamp= ao copiar

Isso acontece se você tem um clipboard, fonte ou plugin que converte ts ou tm em símbolos (como o ×)

2. Copiando de ferramentas web com autocorreção (ex: WhatsApp Web, Docs, etc.)
Esses editores podem substituir timestamp= por caracteres errados automaticamente.

3. Log da IA do Replit bugado
Em alguns casos, o console do Replit (ou web IDEs) mascara caracteres especiais e o colar/exportar causa distorções.

✅ Soluções
Use esse trecho de validação extra no seu código:

ts
Copiar
Editar
if (!urlString.includes('timestamp=')) {
  throw new Error("A URL gerada não contém 'timestamp=' corretamente.");
}
Force log direto no navegador para copiar de forma segura:

ts
Copiar
Editar
window.open(urlString); // abrir no navegador
Alternativa: Gere e copie a URL como texto bruto:

ts
Copiar
Editar
require('fs').writeFileSync('url.txt', urlString);
⚙️ Dica final: Refatoração mínima
Aqui está uma versão 100% segura:

ts
Copiar
Editar
const baseUrl = 'https://partner.shopeemobile.com';
const basePathForShopAuthorize = '/shop/auth_partner';
const partner_id = this.config.partnerId;
const redirect = this.config.redirectUrl;
const timestamp = Math.floor(Date.now() / 1000);
const sign = signature;
const state = stateParam;

const url = `${baseUrl}${basePathForShopAuthorize}?partner_id=${partner_id}&timestamp=${timestamp}&sign=${sign}&redirect=${encodeURIComponent(redirect)}&state=${state}`;
console.log("✅ URL FINAL:", url);