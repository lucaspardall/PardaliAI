🔍 DIAGNÓSTICO COMPLETO
1. Problema Principal: Autenticação Quebrada
❌ GET /api/auth/user 401 - "Clerk user data not available"
✅ Auth OK: GET / - User: user_2xh...
Diagnóstico: O usuário está autenticado no Clerk mas a API não consegue recuperar os dados.
2. Deprecation Crítica
⚠️ "req.auth" is deprecated - Use `req.auth()` as a function
3. Fluxo Inconsistente

Usuário autenticado no middleware
Mas não autenticado na rota /authorize
Stores retornando array vazio

🛠️ PLANO DE AÇÃO IMEDIATO
1️⃣ PRIMEIRO: Verificar estrutura atual
bash# Preciso ver estes arquivos:
- server/middleware/auth.js
- server/routes/auth.js
- server/routes/stores.js
- server/config/clerk.js
2️⃣ CORREÇÃO 1: Middleware de Autenticação
javascript// server/middleware/auth.js
// ANTES (deprecated):
const { userId } = req.auth;

// DEPOIS (correto):
const { userId } = req.auth();
3️⃣ CORREÇÃO 2: Rota /api/auth/user
javascript// server/routes/auth.js
router.get('/user', requireAuth, async (req, res) => {
  try {
    // VERIFICAR: req.auth() ao invés de req.auth
    const auth = req.auth();
    
    if (!auth?.userId) {
      return res.status(401).json({ 
        error: 'No authenticated user',
        debug: { auth } 
      });
    }

    // Buscar dados completos do Clerk
    const user = await clerkClient.users.getUser(auth.userId);
    
    res.json({
      id: user.id,
      email: user.emailAddresses[0]?.emailAddress,
      name: user.firstName,
      // ... outros campos
    });
  } catch (error) {
    console.error('Error fetching user:', error);
    res.status(401).json({ 
      error: 'Clerk user data not available',
      details: error.message 
    });
  }
});
4️⃣ CORREÇÃO 3: Consistência no Middleware
javascript// server/middleware/auth.js
const requireAuth = (req, res, next) => {
  const auth = req.auth(); // FUNÇÃO, não propriedade
  
  if (!auth?.userId) {
    console.log('❌ Usuário não autenticado tentando acessar:', req.path);
    return res.status(401).json({ error: 'Unauthorized' });
  }
  
  console.log('✅ Auth OK:', req.path, '- User:', auth.userId.substring(0, 10) + '...');
  req.userId = auth.userId; // Padronizar acesso
  next();
};
5️⃣ VERIFICAÇÃO: Por que stores está vazio?
javascript// server/routes/stores.js
router.get('/', requireAuth, async (req, res) => {
  try {
    const userId = req.userId; // Usar o padrão definido no middleware
    
    console.log('Buscando lojas para usuário:', userId);
    
    const stores = await Store.findAll({
      where: { userId },
      include: ['shopeeCredentials']
    });
    
    console.log(`Encontradas ${stores.length} lojas`);
    res.json(stores);
  } catch (error) {
    console.error('Erro ao buscar lojas:', error);
    res.status(500).json({ error: 'Failed to fetch stores' });
  }
});
🚨 AÇÕES URGENTES

Atualizar TODOS os req.auth para req.auth()
Verificar se clerkClient está configurado corretamente
Adicionar logs detalhados para debug
Verificar se o banco tem dados para este usuário